/*
#[derive(Copy,Clone,Debug)]
enum FileDescriptorType {
    FD_NONE,
    FD_PIPE,
    FD_INODE,
    FD_DEVICE,
}

struct File {
    fdtype: FileDescriptorType,
    readable: bool,
    writable: bool,
    pipe: Option<Arc<Pipe>>,
    inode: Option<Arc<INodeImpl>>,
    offset: usize,
}
*/
